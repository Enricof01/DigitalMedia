export type SaveState = "idle" | "saving" | "saved" | "error";
export type LoginState = "idle" | "loading" | "ready" | "error";
export type ReportState = "idle" | "sending" | "sent" | "error";
export type AuthMode = "login" | "register";
